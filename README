A .NET client for the neo4j REST API.

Supports basic CRUD operations, Gremlin queries via a fluent interface, and some indexing operations.

Grab the latest CI drop straight from:

http://nuget.org/List/Packages/Neo4jClient.Edge